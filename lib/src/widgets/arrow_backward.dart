import 'package:flutter/material.dart';

class ArrowBackward extends StatelessWidget {
  const ArrowBackward({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
